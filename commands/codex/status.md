---
description: Check that the Codex CLI is installed, authenticated, and ready
---

Call the `codex_status` MCP tool from the codex-in-claude server (it is free — no
model call). Report whether Codex is found, authenticated, and a supported version.
If it is not ready, show the `readiness_detail` and the repair guidance, and do not
call any paid Codex tools until it is resolved.
